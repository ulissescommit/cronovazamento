POSTGRES_DB=cronovazamento
POSTGRES_USER=cronovazamento
POSTGRES_PASSWORD=troque-esta-senha

# chave de cifra das senhas das conexões externas cadastradas no app (Fernet).
# gere a sua com:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# NUNCA reaproveite a chave de exemplo abaixo, e nunca comite a sua de verdade.
CRONOVAZAMENTO_CHAVE_CIFRA=troque-esta-chave-gerada-com-fernet
